APP_NAME=vigiacontratos
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

APP_LOCALE=pt_BR
APP_FALLBACK_LOCALE=pt_BR
APP_FAKER_LOCALE=pt_BR

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# Database (Master)
DB_CONNECTION=mysql
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=vigiacontratos
DB_USERNAME=sail
DB_PASSWORD=password

# Database (Tenant — configurado dinamicamente pelo middleware)
DB_TENANT_HOST=mysql
DB_TENANT_PORT=3306
DB_TENANT_USERNAME=sail
DB_TENANT_PASSWORD=password

# Session (SESSION_ENCRYPT=true recomendado para producao)
SESSION_DRIVER=redis
SESSION_LIFETIME=120
SESSION_ENCRYPT=true
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=redis

CACHE_STORE=redis
# CACHE_PREFIX=

REDIS_CLIENT=phpredis
REDIS_HOST=redis
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_SCHEME=null
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="noreply@vigiacontratos.com.br"
MAIL_FROM_NAME="${APP_NAME}"

# Storage S3-compatible (MinIO em dev / AWS S3 em prod)
AWS_ACCESS_KEY_ID=sail
AWS_SECRET_ACCESS_KEY=password
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=vigiacontratos-dev
AWS_ENDPOINT=http://minio:9000
AWS_USE_PATH_STYLE_ENDPOINT=true

# Multi-Tenant
TENANT_SLUG=
APP_DOMAIN=vigiacontratos.com.br

# Admin SaaS
ADMIN_EMAIL=admin@vigiacontratos.com.br
ADMIN_PASSWORD=
ADMIN_NAME="Admin SaaS"

VITE_APP_NAME="${APP_NAME}"
